<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="timertaskserver.workserver.data.swzzqxsj.Tz_watershedwgData">
    <select id="selectList" resultType="timertaskserver.workserver.pojo.swzzqxsj.Tz_watershedwgPojo">
        select a.* from "tz_watershedwg" a
        <where>
            <if test="ID != null and ID != ''">
                and FID = #{ID}
            </if>
            <if test="key != null and key != ''">
                and NAME like concat('%',concat(#{key},'%'))
            </if>

<!--            <if test="stime != null and stime != ''">-->
<!--                <![CDATA[ and TM >= #{stime} ]]>-->
<!--            </if>-->
<!--            <if test="etime != null and etime != ''">-->
<!--                <![CDATA[ and TM <= #{etime} ]]>-->
<!--            </if>-->
            <if test="type != null and type.size() > 0">
                and TYPE in
                <foreach collection="type" open="(" close=")" item="item" separator=",">
                    #{item}
                </foreach>
            </if>
        </where>
        <if test="startindex != null and pagesize != null">
            limit #{startindex},#{pagesize}
        </if>
    </select>

    <select id="selectCount" resultType="integer">
        SELECT count(1) FROM "tz_watershedwg"
        <where>
            <if test="ID != null and ID != ''">
                and FID = #{ID}
            </if>
            <if test="key != null and key != ''">
                and NAME like concat('%',concat(#{key},'%'))
            </if>
            <!--            <if test="stime != null and stime != ''">-->
            <!--                <![CDATA[ and TM >= #{stime} ]]>-->
            <!--            </if>-->
            <!--            <if test="etime != null and etime != ''">-->
            <!--                <![CDATA[ and TM <= #{etime} ]]>-->
            <!--            </if>-->
            <!--            <if test="type != null and type.size() > 0">-->
            <!--                and GROUPID in-->
            <!--                <foreach collection="type" open="(" close=")" item="item" separator=",">-->
            <!--                    #{item}-->
            <!--                </foreach>-->
            <!--            </if>-->
        </where>
    </select>

    <insert id="insertOne" parameterType="timertaskserver.workserver.pojo.swzzqxsj.Tz_watershedwgPojo">
        insert into "tz_watershedwg"
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="FID != null and FID != ''">
                FID,
            </if>
            <if test="NAME != null and NAME != ''">
                NAME,
            </if>
            <if test="KEYID != null and KEYID != ''">
                KEYID,
            </if>
            <if test="TYPE != null and TYPE != ''">
                TYPE,
            </if>
        </trim>
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="FID != null and FID != ''">
                #{FID},
            </if>
            <if test="NAME != null and NAME != ''">
                #{NAME},
            </if>
            <if test="KEYID != null and KEYID != ''">
                #{KEYID},
            </if>
            <if test="TYPE != null and TYPE != ''">
                #{TYPE},
            </if>
        </trim>
    </insert>

    <update id="updateOne" parameterType="timertaskserver.workserver.pojo.swzzqxsj.Tz_watershedwgPojo">
        update "tz_watershedwg"
        <set>
            <if test="NAME != null and NAME != ''">
                NAME = #{NAME},
            </if>
            <if test="KEYID != null and KEYID != ''">
                KEYID = #{KEYID},
            </if>
        </set>
        where FID = #{FID}  and TYPE = #{TYPE}
    </update>
    <delete id="deleteOne">
        delete from "tz_watershedwg" where FID = #{ID}  and TYPE = #{TYPE}
    </delete>
</mapper>